fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.27.0
pydantic>=2.0.0
agent-client-protocol>=0.11,<1
# Test-only deps: test_challenge.py imports pytest, and the suite is run in CI
# via `python3 -m unittest discover` (no separate pytest install step).
pytest>=8.0.0
pyyaml>=6.0
